import template from './bloglist.html';
import controller from './bloglist.controller';

const blogListComponent = {
    bindings: {},
    template,
    controller
};

export default blogListComponent;
